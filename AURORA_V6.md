# Aurora 06 — 시야와 동선을 다시 설계한 탐험 섬

플레이: https://sungeunstar.github.io/tomob-deploy/sandbox-aurora-v6.html

전체 지형: https://sungeunstar.github.io/tomob-deploy/sandbox-aurora-v6.html?view=overview

이전 01~05 페이지와 본편 game.html은 그대로 두었다. 이번 결과는 새 지형 모듈과 별도 샌드박스이며, 원본 게임 main에 자동 병합하지 않는다.

## 무엇을 바꿨나

소품만 늘리는 대신 해안선, 주요 높이, 능선, 길과 장소 배치를 새로 작성했다. 높이장 영역은 360m, 240분할, 총 58,081 정점이다. 정상 유적 바닥은 높이 64m이며 포탈 아치 상단은 약 76m다.

주 동선은 항구 → 굽은 숲길 → 폭포 조망 → 옛길 폐허 → 마지막 바위 고개 → 정상이다. 다리를 건너 광산과 작업장을 돌아 본선으로 합류하는 순환길, 망루 외곽길, 숨은 해변 우회길을 별도로 두었다. 주 길을 모두 지도처럼 미리 보여주는 방식이 아니다.

항구에서는 정상 아치의 윗부분을 볼 수 있지만 폭포·광산·숨은 해변은 능선 뒤에 둔다. 숲에서 정상은 가려지고, 폭포 앞 코너에서 물줄기가 보인다. 중간 폐허에서 정상 방향을 다시 확인하고, 마지막 바위 고개에서는 한 번 더 가려졌다가 정상에서 열린다. 이 차폐는 안개나 카메라 거리별 숨김이 아니라 실제 충돌 지형으로 만든다. 나무와 바위는 필요한 전망선과 주 이동면을 비켜 배치한다.

기존 본편 player.js, physics.js, input.js, water.js, portalfx.js를 직접 사용한다. 캐릭터 이동 소스는 수정하지 않았다. 기존 광산·선술집·집·물방앗간·대장간·망루 6종, KayKit 나무·돌, the-wharf OBJ 부두를 재배치했다. 정상 포탈 효과도 기존 createPortal을 사용한다.

폭포는 43m 상부 샘 → 30m 중간 웅덩이 → 17m 하부 연못으로 이어진다. 수면은 지형 경계에 맞춰 잘라내며, 기존 바다의 시간·노멀 정보를 공유하는 내륙 물 표현을 유지했다. 좁은 연못에 바다 너울 전체를 적용하지 않는다.

## 파일과 연결

- modules/aurora-expedition-layout.js: 순수 높이장, 8개 경로, 능선, 기존 건물 좌표, 시야 검사.
- modules/aurora-expedition.js: Three.js 지형·기존 에셋·Rapier 충돌·내륙 물·포탈 생성.
- sandbox-aurora-v6.html: 기존 본편 조작으로 들어가는 실행 화면.
- scripts/build-aurora-v6.py: 검수된 v5 기반 에셋 조립 부분에서 v6 모듈을 재생성하는 스크립트. 생성된 모듈은 저장소에 포함돼 있으므로 사용자는 별도 빌드 없이 페이지를 실행한다.

초기화는 새 ctx에서 core → physics → water → input → initAuroraExpedition → player → physics step 순서다. 이미 존재하는 지형을 조용히 덮어쓰지 않는다. 지형 dispose는 소유한 메시·콜라이더와 물 높이 어댑터를 정리한다.

## 검수 기록

코드를 로컬 브라우저에서 실제 WebGL로 렌더링했다. 의존성과 기존 에셋은 앞선 GitHub Actions 런타임 캐시를 사용했다. 전체 지형, 항구·숲·폭포·폐허·마지막 고개·정상 눈높이 장면을 실제 프레임버퍼로 캡처했다. 생성형 콘셉트 이미지를 실행 화면으로 사용하지 않았다.

지형 수치 검사 10개와 실제 충돌 메시 raycast 시야 검사 10개가 기대한 노출/차폐에 일치했다. 정상 대상은 아치의 상단 기준이며, 항구에서 포탈 전체가 항상 보인다는 뜻은 아니다. 마지막 3m의 목표 구조물 자체는 raycast 차폐 검사에서 제외한다. 식생까지 모든 픽셀을 자동 판정한 검사는 아니다.

본편 컨트롤러에 연속 W 입력과 방향을 주고 고정 1/60초 간격으로 진행한 8개 경로 검사가 통과했다. 매 경로의 시작점에서만 배치하고 경로 중간에는 순간이동하지 않았다. 주 동선의 실제 수평 이동거리 약 481m, 협곡·광산 순환길 약 244m를 기록했다. 망루, 숨은 만, 광산, 물방앗간, 대장간, 선술집 접근 지선도 검사했다.

별도 걷기·달리기·점프 검사에서는 이동 약 2.24m, 점프 최고 높이 변화 약 1.77m와 Jump_Idle 클립이 기록됐고 통과했다. 이 경로/점프 검사는 렌더링을 잠시 억제한 고정 스텝 검사다. 실제 이용자가 수동으로 전체 맵을 완주하거나 목표 장비 프레임률을 보장한다는 뜻은 아니다.

로컬 검사에서 새로운 지형/건물 에셋 오류와 페이지 예외 배열은 비어 있었다. 오프라인 캐시에 없는 기존 icon_fist.png 요청 1개는 별도로 기록했다. 외부 네트워크 요청 전체가 성공했다고 주장하지 않는다.

## 남은 범위

생성한 콘셉트 이미지와 같은 최종 아트 품질을 보증하지 않는다. 이번 핵심 변경은 실제 지형에 의한 순차 공개와 연속 보행 가능한 동선이다. 가까이서 보이는 재질·바위 형태, 전체 수동 카메라 체감과 성능은 추가 개선 대상이다.

광산 내부, 벌목/채광 자원 등록, 상점·생산, 포탈의 던전 전환, 퀘스트·보상, 소유권, 선박 정박, 본편 월드 스트리밍과 세이브는 새로 연결하지 않았다. 폭포 음향 이벤트도 이번 빌드에는 없다. 장소 메뉴는 검수용 순간이동이며 실제 탐험 경로를 대체하지 않는다.
