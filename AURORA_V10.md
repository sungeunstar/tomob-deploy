# Windgate 10 — 성소 아래 굽은 순례굴

## 바로 실행

- 굴 입구에서 시작: https://sungeunstar.github.io/tomob-deploy/sandbox-aurora-v10.html?place=vault-entry&rev=59edc30991
- 항구에서 시작: https://sungeunstar.github.io/tomob-deploy/sandbox-aurora-v10.html?rev=59edc30991
- 전체 보기: https://sungeunstar.github.io/tomob-deploy/sandbox-aurora-v10.html?view=overview&rev=59edc30991

검증된 생성 파일 커밋: `8fc8c49d61325fe68be7c67f62b02dc761356847`.
검사 입력 커밋: `59edc30991cd5908ae2335309d0ae0a1226051ef`.
GitHub Actions 실제 브라우저 검사: run `36243428214`, artifact `10906517567`.
빌드 이름: `aurora-v10-undercroft`.

## 이번 구현

v8/v9 ZIP의 장식용 통로를 그대로 덮어쓴 것이 아니라, 검증된 v7을 기반으로 새 지형 절개와 통로를 구현했다. 이전 미리보기는 보존한다.

성소 아래를 굽어서 통과하는 약 39.2m 중심선의 순례굴을 추가했다. 입구와 출구가 바로 관통해 보이는 직선 통로가 아니라 두 번 방향이 바뀌는 경로다. 원래 정상으로 올라가는 외부 산길은 유지하므로 굴을 통과하거나 기존 길을 선택할 수 있다.

지형의 해당 삼각형을 실제로 잘라내고 동일한 메시로 Rapier 충돌을 생성한다. 정상의 지붕 지형은 남기며, 내부 바닥·벽·천장을 별도로 만든다. 장식용 아치 앞의 투명화나 순간이동으로 통과를 흉내 내지 않는다.

입구 바닥과 외부 지형의 높이를 맞추고, 터널 벽을 지형 경계에서 삼각형 단위로 잘라 톱니처럼 남던 면을 보정했다. 벽·바닥에 풍화 재질, 오래된 석조 보강부, 벽등을 추가했다. 파편은 중심 보행로가 아닌 벽 가장자리에 놓는다.

기존 막대형 표지판 대신 4개 갈림길에 총 8개 방향판을 배치했다. 성소·광산·망루·해변·항구·옛 순례굴·옛길을 실제 분기 방향으로 가리킨다. 글자와 목재 표현은 하나의 아틀라스를 공유한다. 경로 바깥의 식생·잔돌 경계도 보강했다.

광산·건물·부두·나무·돌·포탈은 기존 게임 에셋과 모듈을 유지한다. 본편 `player.js`, `physics.js`, `input.js`는 수정하지 않는다. 굴 주변에서만 물리적인 벽에 의해 막히는 3인칭 카메라를 안쪽으로 당기는 샘플 어댑터를 추가했다.

## 실제 검사 결과

Chromium / SwiftShader에서 실제 WebGL 캔버스를 17개 PNG로 캡처했다. 생성형 이미지가 아니다.

본편 입력과 Rapier 고정 스텝으로 아래 10개 경로를 연속 이동했다. 각 경로 시작에만 위치를 설정하고 웨이포인트 사이에는 순간이동하지 않았다.

| 경로 | 측정 이동거리 | 결과 |
|---|---:|---|
| 순례굴 진입 → 출구 | 37.67m | 통과 |
| 순례굴 역방향 | 37.61m | 통과 |
| 항구 → 정상 기존 주 경로 | 480.71m | 통과 |
| 협곡 순환길 | 244.23m | 통과 |
| 광산 접근 | 11.63m | 통과 |
| 물방앗간 접근 | 20.11m | 통과 |
| 작업장 접근 | 14.27m | 통과 |
| 망루 길 | 96.50m | 통과 |
| 숨은 해변 길 | 115.45m | 통과 |
| 항구 마당 | 18.60m | 통과 |

39.2m는 설계 중심선 길이이며 실제 보행 검사는 웨이포인트 허용 반경을 사용해 약 37.7m를 이동했다.

걷기·달리기·점프·착지 검사도 통과했다. 걷기 변화 2.2398m, 점프 최고 높이 변화 1.7667m, Walking_A / Running_A / Jump_Idle 및 착지 후 Idle_A가 기록됐다.

기존 10개 표본 시야 조건 모두 통과했다. 굴 내부 두 지점 사이의 직선 시야가 실제 벽에 막히며, 다섯 위치에서 머리 위에 실제 천장 메시가 존재하는 것도 확인했다.

신규 런타임·셰이더·모델 로딩 오류는 기록되지 않았다. 기존 빈손 UI 아이콘 `ui/icons/icon_fist.png`의 HTTP 404는 남아 있다.

고정 스텝 이동 검사 중 렌더링은 잠시 억제한다. 이것은 사용자 PC의 실시간 FPS 벤치마크, 모든 임의 이동 경로, 모든 카메라 각도의 완전한 수동 검수가 아니다.

## 코드

- `modules/aurora-passage.js`: 새 샌드박스의 `initAuroraPassage(ctx)` 진입점.
- `modules/windgate10-vault-runtime.js`: 실제 지형 절개, 굴, 표지판, 주변 경계 및 카메라 어댑터.
- `modules/windgate10-polish.js`: 통로와 충돌하지 않는 지질 보정.
- `modules/windgate10-vault.js`: 저작 원본.
- `scripts/build-windgate10.mjs`, `scripts/finish-windgate10.mjs`: 정확한 v7 입력 해시를 확인하고 생성하는 재현 절차.
- `scripts/qa-windgate10.mjs`: 실제 렌더·본편 입력·경로·시야 검사.
- `sandbox-aurora-v10.html`: 독립 실행 화면.

기존 `game.html`, `sandbox.html`, 01~07 미리보기와 세이브는 변경하지 않았다. 모듈은 비어 있는 지형 컨텍스트에서 물리·물 초기화 후, 플레이어보다 먼저 초기화한다.

## 남은 범위

컨셉 아트와 같은 최종 아트 품질을 보증하는 버전이 아니다. 입구 측면의 지형 이음, 큰 암반의 형태와 주변 돌 배치에는 추가 시각 보정 여지가 남아 있다. 자동 통과 검사가 시각적 완성도를 뜻하지 않는다.

NPC·벌목·채광·상점·포탈 던전 전이·본편 월드 편입·세이브 연결을 새로 추가한 것은 아니다. 이번 범위는 실제 통과 가능한 굴과 동선·표지판 고도화 및 배포다.
